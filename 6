#include <iostream>
#include <math.h>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	double a, b, c;
	cout << "Введите а,b,c" << endl;
	cin >> a >> b >> c;
	if (a * a + b * b == c * c) {
		cout << "Удовлетворяет" << endl;
	}
	else {
		cout << "Не удовлетворяет" << endl;
	}
}
